export type CellPos = { r: number; c: number };

export type RegionMap = number[][] | null;

export type SolutionGrid = number[];

export type DifficultyRating = { label: string; stars: number; score: number } | null;
